# Decisions (PM-owned · append-only · one entry per decision)

- **D1 — NPS tenancy scope:** `NpsSurvey` is franchisee-isolated (FranchiseeId filter) with BrandId+TerritoryId denormalized for the dashboard grain. *Why:* shares the appointment's isolation boundary; stays territory-resolvable without a join. (slice-c)
- **D2 — RBAC role claim: DEFERRED (Track 2).** bravo derives corporate-vs-franchisee from presence/absence of the `franchisee_id` claim, so no role claim is needed for the demo and no spoofable header is reintroduced. (slice-a follow-on parked)
- **D3 — bravo merges with the stub before its EF swap.** EF swap deferred to a post-alpha follow-up; bravo touches no AppDb/Domain/Seed so it doesn't block on alpha's schema. (Now alpha is merged → EF swap unblocked.)
- **D4 — Slug↔int bridge: `franchisee_slug`** added to `territory_period_summary` (CONTRACT v1.2, additive). alpha owns it; bravo maps the token slug → this column for the franchisee lens. v1.1 reserved for bravo's `/api/dashboard/map`.
- **D5 — CI is the merge authority.** Local self-reports are a pre-check; the PR's CI run gates the merge. (Proven: alpha self-reported 8/8 green, PR conflict-clean, merged → CI caught a real red on integrated main.)
- **D6 — Modularize after round 1.** The hub-file refactor (wt-modularize) runs only after bravo/charlie/echo land, branched off the settled main — running it mid-round would collide with every lane.
- **D7 — Smoke assertions assert a floor, not exact seed counts.** Fixed `franchisee catalog` check from `==16` to `>=16` after alpha's reseed grew it to 38 (PR #5).
- **D8 — Generic NATO slots.** Worktrees are reusable slots (alpha..echo); task/role/allowed_paths/skills reassigned per round via the bus. Physical dir rename (slice-a→delta, slice-d→echo) only when the slot's window is closed and (for echo) after its work merges.

- **D9 — Theme toggle PARKED (2026-06-15).** charlie's Phase 3 light/dark toggle is non-load-bearing polish for an interview demo; parked in the roadmap, not assigned. Revive only on idle. *Why:* round is green + demo-ready; effort better spent landing the one unlanded refactor than adding surface.
- **D10 — Modularize LANDS NOW (2026-06-15).** wt-modularize (Seed/Config split) is 25 behind with the API round closed → this is the low-conflict window. Rebase onto main, re-verify (build + smoke), pure-refactor PR. *Why:* D6's "after round 1" condition is met; deferring further only rots the branch against every new API change.

- **D11 — NPS-measured seeding LANDS post-refactor (2026-06-15).** The "flip seeded territories Illustrative→Measured" product touch (alpha's `SeedDashboardNpsSurveys`) was NEVER merged — `origin/main:api/Seed.cs` has zero NpsSurvey rows, so network NPS reads Illustrative for all. The work is preserved as `.pm/parked/nps-measured.patch` (and alpha's stash). PM lands it as a fresh PR AFTER chore-modularize's `Seed.cs → Seed/**` refactor merges, re-applied onto the new structure (avoids the Seed.cs collision). Honest: only the 6 listed territories (2 per dashboard brand) get real surveys tied to completed appointments; all others keep illustrative values. *Why:* already-built, defensible, strengthens the provenance story; sequenced to not fight the active api refactor.

- **D12 — Back-Office Wave 1 (2026-06-16).** After deploying the #46 theme redesign live, the demo expands into a franchisor back-office at `/back-office` (corporate-scope). Flagship = a **full custom report builder** (metric × dimension × period → run → table+chart → CSV/XLSX export → save; schedule/share = coming-soon). Plus **Territory Explorer + scorecard**, and **Users&Roles / Org&Catalog** sections (mostly coming-soon stubs). 6 disjoint lanes (`docs/backoffice/ROADMAP.md`), 2 frozen contracts (`docs/backoffice/CONTRACTS.md`: route/nav owned by Foundation, reporting API owned by alpha). *Why:* user wants enterprise back-office functionality with custom reporting as the key area; disjoint ownership + Foundation-first sequencing keeps the one shared surface (routing/shell) collision-free.
- **D13 — Bravo's accent-only theme rejected; charlie's full theme (#46) shipped (2026-06-16).** `feat/dark-hfc-accents` (HFC-accent the navy, keep base) was superseded by the full light/dark token system (#46), which the user reviewed and approved. Accent branch preserved for history; bravo repurposed to Territory Explorer. charlie's uncommitted exceljs export WIP preserved in its `stash@{0}` and folded into the Reports lane.
- **D14 — Deploy is code-only against existing infra (2026-06-16).** Redesign deployed by pushing code (build SPA → bundle → publish API → zip-deploy → health-gate → dispatch e2e) to the existing Central US App Service; Bicep was SKIPPED because the RG metadata location (`eastus2`) disagrees with where its resources live (`centralus`), making `az group create`/Bicep collide. delta owns making `deploy.sh` reconcile this + add a first-class `--code-only` mode. *Why:* infra already exists and is live; re-provisioning is unnecessary and currently broken by the region inconsistency.

- **D15 — Back-office lint gate + prettier hygiene (2026-06-16).** delta's CI adds a lint check scoped to `web/src/app/backoffice/**`, landed **non-blocking** (`continue-on-error`) because Foundation (#48) introduced 8 format-drifted files. Standing rule: **web lanes run `npx prettier@3 --write` on their `backoffice/**` output before PR.** chore-modularize owns the one-time cleanup of the 6 foundation files (assignment `m2`); once `main`'s `backoffice/**` is clean, delta flips the gate to hard (delta follow-up). *Why:* keep the new gate honest without red-blocking every in-flight PR mid-round.

- **D16 — Scheduling cross-tenant picker REMOVED; reverses O2 (2026-06-17).** The user explicitly rejected the booking picker showing all franchisees ("I don't want brands to see other brands once they are signed in"). O2 had accepted it as demo-acceptable; that is now overruled. PM hotfix PR #55 (`fix/scheduling-cross-tenant-picker`, web/src/app/app.{ts,html}) removes the picker on `/booking` and drives the page off the existing `/login` franchisee session. `/api/franchisees` stays anonymous (it IS the pre-login directory) — this was a UI-exposure bug, not an API leak. CI green; verified in-browser locally. *Why:* explicit user requirement > prior convenience decision.

- **D17 — #51 Report Builder GHOST-MERGED; charlie re-lands + Playwright now runs locally (2026-06-17).** PR #51 shows MERGED but `origin/main` has only the ComingSoon **stub** + none of charlie's 6 reports/ files — a tip-only merge (the `.pm/risks.md` pattern, caught again). The real work is intact on `feat/report-builder-ui@e84b9ba`; charlie rebases it onto main, flips mock→live (#50), and MUST verify the artifact actually lands (diff --stat shows 6 files, not the stub) before+after merge. Separately: the long-standing "browser e2e is CI-only (missing libnspr4)" constraint is SOLVED in-sandbox without sudo — extract `libnss3`/`libnspr4`/`libasound2t64` debs to `~/.local/pw-libs/lib` + `LD_LIBRARY_PATH`, drive same-origin via `api/wwwroot` on :5180. echo wires this into the e2e harness so drivers become a real pre-merge check, not CI-only. *Why:* the flagship feature must actually exist on main; local browser e2e closes the gap that let UI bugs (the booking picker, the ghost merge) slip past the API smoke gate.

- **D18 — STABILIZATION round before new features (2026-06-17).** User: "iron out all the bugs and make sure all the brands are seeded with good data before we move on to new features." No new surface area this round. Verified bug list assigned across all 6 lanes (outbox `2026-06-17s__*`): charlie=re-land builder, bravo=operator-dashboard stale-drawer+flat-sparkline, chore-modularize=back-office home mislabel, alpha=all-brand seed-quality audit + reporting integrity + EF-swap assessment, delta=dev-db/lint hardening, echo=rebase #53 + local browser e2e + verify. Seed coverage pre-checked healthy (all 8 brands, both planes). *Why:* correctness/quality pass on the live demo takes priority over expansion; disjoint paths keep all 6 parallel.

## RESOLVED (were OPEN)
- **O1 — RESOLVED.** Franchisee dashboard shipped (PR #8); corporate kept `web/src/app/dashboard/`, no collision. Dir-split moot.
- **O2 — SUPERSEDED by D16 (2026-06-17).** Previously "left as-is" (booking picker shows all franchisees). User overruled it; picker removed in PR #55. The underlying `/api/franchisees` stays anonymous (pre-login directory) by design.
